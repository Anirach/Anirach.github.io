# Research ledger — post #7 `hermes-desktop-safety`

**Series:** Hermes Desktop Hands-On 2026 (`scripts/series/hermes-desktop.json`) · **Post:** #7 of 7, the series close — "Safety, Remote & Recovery — ปลอดภัย ต่อระยะไกล กู้คืนได้" / "Safety, Remote & Recovery — Safe Hands, Remote Hosts, and Getting Back Up" · **Chip:** #7 Recovery
**Written:** 2026-09-07 · **Access date for every source below:** 2026-09-07 (Asia/Bangkok) · **Sources fetched this run:** 21 (14 cited in the post as [1]–[14]; 7 fetched for verification only)
**Sheets:** `.bilingual/hermes-desktop-safety.th.html` · `.bilingual/hermes-desktop-safety.en.html` · **Figure:** `safety-remote-recovery` (`images/hermes-desktop-fig-07-safety-remote-recovery.png`, not drawn at the time of writing)

> Binding rule: the post may not contain a number, date, command, path, quotation or URL that
> does not appear in this ledger.

Fetch method: raw markdown from `raw.githubusercontent.com/NousResearch/hermes-agent/main/…` (cache-busted with `?nc=`), GitHub API through `gh api` (anonymous `curl` to `api.github.com` returned 403 rate-limit on this run), YouTube oEmbed through `curl`. Every fetched copy is saved under the session scratchpad `hd/src/`.

---

## Sources

| N | Tag | Title | URL (linked in the post) | Fetched from | Publisher | Published | Accessed | Supports |
|---|---|---|---|---|---|---|---|---|
| [1] | Docs | Security — Hermes Agent User Guide | `https://hermes-agent.nousresearch.com/docs/user-guide/security` | `…/main/website/docs/user-guide/security.md` (44,275 bytes, md5 `9da3f442…`) | Nous Research | docs `main` as of 2026-09-07 | 2026-09-07 | §1 all three dials; §2 the `approvals:` block, modes, YOLO, blocklist, deny, timeout, the CLI prompt, container bypass, `approvals suggest`; the write guards and `HERMES_WRITE_SAFE_ROOT`; the seven `terminal.backend` values |
| [2] | Docs | Hermes Desktop — Hermes Agent User Guide | `https://hermes-agent.nousresearch.com/docs/user-guide/desktop` | `…/main/website/docs/user-guide/desktop.md` (54,936 bytes) | Nous Research | docs `main` | 2026-09-07 | status-bar YOLO toggle; Safety page listed but undescribed; Applies to chips; update flow and order; uninstall scopes; remote-backend recipe; `HERMES_DESKTOP_REMOTE_URL`; error card and Send diagnostics; `desktop.log`; reset commands; SSH host key; Electron download; TCC |
| [3] | Docs | Connecting Desktop to Many Hermes Instances | `https://hermes-agent.nousresearch.com/docs/user-guide/multi-connection-desktop` | `…/main/website/docs/user-guide/multi-connection-desktop.md` (18,027 bytes) | Nous Research | docs `main` | 2026-09-07 | §4 Add connection steps, kinds, Test/Reachable, Primary, gateway selector; §7 Update all instances; table rows on Connection test failed |
| [4] | Docs | CLI Commands — Hermes Agent Reference | `https://hermes-agent.nousresearch.com/docs/reference/cli-commands` | `…/main/website/docs/reference/cli-commands.md` (105,184 bytes) | Nous Research | docs `main` | 2026-09-07 | `hermes logs` names/files/options; `hermes doctor --fix`; `hermes debug share` flags; `hermes uninstall` flags incl. `--dry-run`; `hermes serve` description |
| [5] | Docs | Updating & Uninstalling — Getting Started | `https://hermes-agent.nousresearch.com/docs/getting-started/updating` | `…/main/website/docs/getting-started/updating.md` (21,405 bytes) | Nous Research | docs `main` | 2026-09-07 | §7 update steps, snapshot, stage-and-swap, serve relaunch, `--check`/`--plan`/`--backup`, receipts, Windows guards, `--keep-stash`, rollback, Docker refusal, backup vs export; §3 alert |
| [6] | Docs | Checkpoints and /rollback | `https://hermes-agent.nousresearch.com/docs/user-guide/checkpoints-and-rollback` | `…/main/website/docs/user-guide/checkpoints-and-rollback.md` | Nous Research | docs `main` | 2026-09-07 | §3 in full |
| [7] | Docs | Hermes Web Dashboard | `https://hermes-agent.nousresearch.com/docs/user-guide/features/web-dashboard` | `…/main/website/docs/user-guide/features/web-dashboard.md` | Nous Research | docs `main` | 2026-09-07 | table row: `/api/ws` close codes 4401 / 4403 |
| [8] | Docs | SECURITY.md — Hermes Agent Security Policy | `https://github.com/NousResearch/hermes-agent/blob/main/SECURITY.md` | `…/main/SECURITY.md` (15,752 bytes, md5 `b18782c6…`) | Nous Research | repo `main` | 2026-09-07 | §1 blockquote; §2 alert on what a backend confines; §6 blockquote on private reporting; takeaway on vulnerabilities |
| [9] | Docs | apps/desktop/README.md — Hermes Desktop | `https://github.com/NousResearch/hermes-agent/blob/main/apps/desktop/README.md` | `…/main/apps/desktop/README.md` (10,404 bytes) | Nous Research | repo `main` | 2026-09-07 | the execution-boundary sentence (§1, §4 step 5, takeaways) |
| [10] | Release | Hermes Agent v0.21.0 (v2026.8.31) — The Pantheon Release | `https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.31` | `gh api repos/NousResearch/hermes-agent/releases/tags/v2026.8.31` (42,294 bytes) | Nous Research | 2026-08-31T19:29:49Z | 2026-09-07 | TCC identity #95091; detached update hand-off / "Updating Hermes"; dry-run approval checker mention; protected instruction files; redaction sweep |
| [11] | Issue | #89675 Desktop: no sessions load for any agent profile after update — backend spawned without --profile | `https://github.com/NousResearch/hermes-agent/issues/89675` | `gh api …/issues/89675` | NousResearch/hermes-agent | opened 2026-08-19T03:02:34Z; closed 2026-08-23T04:24:32Z (`state_reason: completed`) | 2026-09-07 | table row 3 |
| [12] | Issue | #88275 [desktop] Renderer process burns 40-70% CPU at idle since early August — thermal throttling on macOS Intel | `https://github.com/NousResearch/hermes-agent/issues/88275` | `gh api …/issues/88275` | NousResearch/hermes-agent | opened 2026-08-17T08:48:03Z; open | 2026-09-07 | table row 6 |
| [13] | Issue | #40656 Desktop remote gateway can destabilize self-hosted host when used concurrently with TUI/gateway | `https://github.com/NousResearch/hermes-agent/issues/40656` | `gh api …/issues/40656` | NousResearch/hermes-agent | opened 2026-06-06T16:26:03Z; open | 2026-09-07 | §4 alert; takeaway on Remote |
| [14] | Community | Kudryk, Igor. *Full Guide to Hermes Agent Desktop App (best way to run Hermes?)* | `https://www.youtube.com/watch?v=QxotQWwB7ws` | `https://www.youtube.com/oembed?url=…QxotQWwB7ws&format=json` (HTTP 200: title, author_name "Igor Kudryk", author_url `youtube.com/@igorkudryk`) | YouTube | not exposed by oEmbed; the research pack dates it 2026-06-12 | 2026-09-07 | §2 alert — the paraphrased practitioner point that the SSH backend relocates the terminal only |

### Fetched for verification only (not cited)

| Title | URL fetched | Status | Why |
|---|---|---|---|
| Configuration — `terminal:` block | `…/main/website/docs/user-guide/configuration.md?nc=…` | 200 (197,007 bytes) | Confirms `backend: local    # local \| docker \| ssh \| modal \| daytona \| vercel_sandbox \| singularity`; the post cites the same seven names from [1]'s comparison table instead |
| Environment Variables | `…/main/website/docs/reference/environment-variables.md?nc=…` | 200 | Confirms `HERMES_YOLO_MODE` ("Set to 1 to bypass dangerous-command approval prompts. Equivalent to --yolo"), `HERMES_DESKTOP_REMOTE_URL`, `HERMES_WRITE_SAFE_ROOT`, `HERMES_DASHBOARD_BASIC_AUTH_SECRET` — all also stated in [1]/[2], which the post cites |
| Slash Commands reference | `…/main/website/docs/reference/slash-commands.md` | 200 | Confirms `/yolo` ("Toggle YOLO mode — skip all dangerous command approval prompts"), `/approvals [manual\|smart\|off]`, `/approve [session\|always]` (messaging-only), `/rollback [number]`, and that `/rollback` and `/yolo` work in both CLI and gateway |
| FAQ & Troubleshooting | `…/main/website/docs/reference/faq.md` | 200 | "Command blocked as dangerous" entry (the `rm -rf` example, "Hermes never silently runs destructive commands"); no Desktop-specific troubleshooting entries exist there |
| Website sidebar | `…/main/website/sidebars.ts` | 200 | Located the checkpoints page id `user-guide/checkpoints-and-rollback` |
| PR #81137 `hermes approvals test` — dry-run approval verdict CLI | `gh api …/pulls/81137` | 200; merged 2026-08-07T15:57:40Z | The release notes [10] call the feature `hermes approval-check`; the merged PR body calls it `hermes approvals test`; the CLI reference [4] lists neither. Recorded here so the post hedges rather than prints a command (see Do not assert) |
| Issue #73082 [Bug]: Desktop client renderer/GPU processes spin at 100%+ CPU at idle | `gh api …/issues/73082` | 200; opened 2026-07-28T03:18:15Z, closed completed 2026-08-21T11:09:02Z; labels `type/perf, P2, comp/desktop, area/usage-cost`; Apple Silicon, agent 0.19.0 / desktop 0.18.0 | The Apple-Silicon twin of #88275; not cited — the table row is about the still-open Intel case (see Verification record) |

---

## Verification record

- **[1] docs Security page.** First `curl` of `website/docs/user-guide/security.md` returned the repo's `SECURITY.md` body (15,752 bytes, md5 identical to `SECURITY.md`) — a stale edge cache. Re-fetched with `?nocache=$RANDOM` → HTTP 200, 44,275 bytes, frontmatter `title: "Security"`, 13 occurrences of `approvals`; byte-identical to the copy another sweep pulled on 2026-09-06. All quotations in §1–§2 were read from this second copy (`hd/src/docs-security.md`). Checked verbatim: the 8-line `approvals:` block; the modes table; "Setting `approvals.mode: off` disables all safety prompts"; the three YOLO activation methods; `⚡ YOLO mode ON — all commands auto-approved. Use with caution.` and `⚠ YOLO mode OFF`; the red banner and `⚠ YOLO` status fragment; the :::danger block "YOLO mode disables **all** dangerous command safety checks for the session — **except** the hardline blocklist"; the four bullets the blocklist survives; "there's no override flag"; the six blocklist rows; the `approvals.deny` three-example block; "before `--yolo`, `/yolo`, and `approvals.mode: off` are consulted"; "matched **case-insensitively** against the whole command text"; "Deny rules apply to host-reaching backends"; "the command is **denied** by default (fail-closed)"; "changes take effect immediately (the config cache is mtime-keyed)"; the CLI prompt `⚠️ DANGEROUS COMMAND: recursive delete / rm -rf /tmp/old-project / [o]nce | [s]ession | [a]lways | [d]eny`; the container-bypass :::info; `hermes approvals suggest` and "`rm -rf build/` approved 100 times still never yields an `rm` entry"; the protected-paths table; "there is no approval prompt and no way to override from the chat UI"; the seven-row Terminal Backend Security Comparison table.
- **[2] desktop.md** HTTP 200. Checked verbatim: "Per-session YOLO toggle — flip YOLO on or off for just this session (matching the TUI)"; the per-profile pages list "Model, Workspace, Safety, Memory & Context, Voice, Chat, Advanced, and Tools & Keys" and the Applies-to rules (default follows the active profile; switching resets it; hidden under two profiles); Updating: "checks for updates in the background and offers a one-click update", the order "the connected backend first, then every other eligible registered gateway (Hermes Cloud entries are platform-managed and skipped), and the desktop app itself last", "Update desktop app"; Uninstalling: the three Danger-zone labels and their CLI equivalents, "The app closes to finish the job", "agent-removing options are hidden automatically when no local agent is installed", the source-checkout note; the remote-backend section: provider choice (OAuth "preferred for anything reachable beyond your own machine", `hermes dashboard register`, "Sign in with Nous Research", self-hosted OIDC; username/password "local / trusted-network use only"), the `.env` heredoc and `hermes serve --host 0.0.0.0 --port 9119`, "The desktop app does not start it for you", the gateway-is-separate note, the Tailscale/VPN warning, In the app steps 1–3, `HERMES_DESKTOP_REMOTE_URL`, the `curl … | jq '.auth_required, .auth_providers'` check ("should report `true` and include `"basic"`"), "Signed out on every restart — set `HERMES_DASHBOARD_BASIC_AUTH_SECRET`"; Troubleshooting: the error-card layers and five actions, "uploads a redacted debug bundle to Nous-internal storage after an explicit consent prompt (same pipeline as `hermes debug share --nous`; secrets are always redacted, the bundle is viewable by Nous staff only and auto-deletes after 14 days)", "Open Desktop logs" on remote/Cloud, `HERMES_HOME/logs/desktop.log`, `hermes logs gui -f`, the three reset commands, `ssh-keygen -R <host>` + Retry, the Electron section ("~114 MB", `retrying attempt=…`, self-heal via npmmirror.com, "the build never overrides one you've set", the two `rm -f … electron-*.zip` lines); TCC: grants keyed to the signing identity, "Locally built and self-updated apps are signed with a stable identifier-pinned ad-hoc signature, so grants persist across updates", the one-time note that grants made to builds *before* the identifier-pinned signing fix (PR #73681) "carry the old cdhash-pinned requirement" and re-prompt while macOS still shows the toggle ON, `tccutil reset ScreenCapture com.nousresearch.hermes`, `hermes desktop --setup-tcc-identity`, "Hermes Local Signing", `tccutil reset All com.nousresearch.hermes`.
- **[3] multi-connection-desktop.md** HTTP 200. Checked: the three doors; the four-kind table; "unique device name … case-insensitive"; Primary semantics ("registry fallback … does not switch the current Sessions workspace; removing the primary falls back to the local entry"); "Test probes the connection's own HTTP and WebSocket legs … 'Reachable' toast"; Adding a connection steps 1–6 with the Name (max 64), Gateway URL example `http://homelab.lan:9119`, Session token / OAuth, SSH `user@host:22` and "the app adopts a dashboard token over the tunnel"; the gateway selector and `(gateway, profile)` scoping; Update all instances (shown once more than one connection is registered; Local / Remote and SSH / Hermes Cloud "Managed by Hermes Cloud"; "Docker, Nix refuse politely"); Troubleshooting rows "Connection test failed" and "An agent shows but won't open".
- **[4] cli-commands.md** HTTP 200. Checked: the `hermes logs` table (agent/errors/gateway/gui/desktop with file names and descriptions), "All logs are stored in `~/.hermes/logs/` (or `<profile>/logs/` for non-default profiles)", the `list` argument, `-n`, `-f`, `--since`; `hermes doctor [--fix]`; `hermes debug share` with `--nous`, `--local`, `--no-redact`, "By default, uploads are redacted", "auto-deletes after 14 days"; the maintenance table row `hermes uninstall [--full] [--gui] [--dry-run] [--yes]`; `hermes serve` "the JSON-RPC/WebSocket gateway the desktop app and remote clients connect to … a non-loopback bind engages the same auth gate", default port 9119. Note: the CLI reference has no `## hermes uninstall`, `## hermes desktop` or `## hermes approvals` section; `hermes desktop (alias gui)` appears only in the command index table.
- **[5] updating.md** HTTP 200. Checked: steps 1–7 (quick snapshot contents; step 6 "Desktop rebuild (stage-and-swap)" and "`⚠ Update partially complete` and `hermes desktop` retries the rebuild"; step 7 manually launched `hermes serve` "relaunches it afterward on the **same host and port**"); `--check` ("No files are modified, no gateway is restarted"); `--plan`; receipts at `~/.hermes/logs/update_receipts/` with `latest.json`; `--backup` and `updates.pre_update_backup: quick | full | off`; "Desktop updates never auto-restore … `hermes update --keep-stash`"; the Windows `hermes.exe` guard and the venv guard "not bypassed by `--force`; … `hermes update --force-venv`"; Rollback instructions (`git log --oneline -10`, `git checkout <commit-hash>`, `uv pip install -e ".[all]"`, `hermes gateway restart`, `hermes config check`); the Docker provenance marker ("refuse cleanly (exit code 2), print the actual update command (`docker pull nousresearch/hermes-agent:latest`)"); the uninstall tip on `hermes backup` vs `hermes profile export`.
- **[6] checkpoints-and-rollback.md** HTTP 200. Checked: "Checkpoints are **opt-in** as of v2 … the default is off"; `hermes chat --checkpoints`; the config block with defaults 20 / 500 / 10; the store path; triggers list; "at most one checkpoint per directory per turn"; the `/rollback` quick-reference table; "Undoes the last conversation turn"; the pre-rollback snapshot; the agent-write ledger and `--all`; guards (git on PATH, root/home skipped, 50,000 files, per-file cap, 500 MB cap); `hermes checkpoints` / `prune` / `clear`.
- **[7] web-dashboard.md** HTTP 200 (re-fetched with `?nc=`). Line 202: "look for the `/api/ws` close code (4403 = chat WS rejected by the request guard, e.g. Host/peer mismatch; 4401 = the WS ticket didn't authenticate)" and "grab a fresh `desktop.log` (Settings → Gateways → Open logs)".
- **[8] SECURITY.md** HTTP 200 (two fetches, md5 identical). Checked: §1 reporting sentence, "Do not open public issues", "**Hermes Agent does not operate a bug bounty program.**"; §2.2 "**The only security boundary against an adversarial LLM is the operating system.**"; §2.2 Terminal-backend isolation "What this confines … What this does **not** confine: everything the agent does in its own Python process. That includes the code-execution tool …, MCP subprocesses …, plugin loading, hook dispatch, and skill loading"; §2.4 heuristics; §5 "90 days from report, or until a fix is released, whichever comes first".
- **[9] apps/desktop/README.md** HTTP 200. Lines 146–148 verbatim: "In remote mode the gateway host is the execution boundary: agent tools, terminal commands, and file operations run against the remote Hermes host, not the computer displaying the Desktop UI."
- **[10] v0.21.0 release** via `gh api`, `tag_name` `v2026.8.31`, `name` "Hermes Agent v0.21.0 (v2026.8.31)", `published_at` 2026-08-31T19:29:49Z. Lines checked: "**macOS TCC identity** — permission grants survive every update via `hermes desktop --setup-tcc-identity` (#95091)"; "**Detached update hand-off on every OS** — quit → update → reopen with a single shim window; Windows desktop updates no longer park on "Updating Hermes" (#83634, #90937, #95897)"; "**`hermes approval-check`** — dry-run approval verdicts for a command without running it (#81137)"; "Protected agent-instruction files (AGENTS.md, skills, memory stores) now always require write approval"; "A deep redaction sweep closed secret-leak gaps across terminal errors, `.env` file reads, checkpoints, and ACP logs".
- **[11] #89675** via `gh api`: `state` closed, `state_reason` completed, labels `type/bug, P1, sweeper:risk-session-state, sweeper:risk-compatibility, comp/desktop, area/profiles`, 21 comments. Body: "Hermes CLI: v0.20.4 (2026.8.18)"; "the app connects to a remote backend that was spawned **without the `--profile` flag**, so it queries the global/empty session store"; "Workaround: use the WebUI dashboard (`hermes dashboard`, port 8787) which handles profiles correctly" — the port number is omitted from the post because it conflicts with the documented default 9119.
- **[12] #88275** via `gh api`: `state` open, labels `type/perf, P2, comp/desktop`, 12 comments. Body: macOS 13.3.1, MacBook Pro 2019 (Intel Core i5-8279U), "Hermes v0.20.2 (2026.8.16), Electron 40.10.2"; "`Hermes Helper (Renderer)` consumes **40-73% CPU continuously while idle**"; "`desktop.disable_gpu=true` partially mitigates: ~70% → ~40%". `desktop.disable_gpu` does not appear in configuration.md, desktop.md or environment-variables.md — the post calls it undocumented.
- **[13] #40656** via `gh api`: `state` open, labels `type/bug, comp/gateway, tool/mcp, P3`, 1 comment, created 2026-06-06T16:26:03Z. Body: "Hermes Agent: `v0.16.0 (2026.6.5)`"; "The remote host's interactive `hermes` TUI and/or gateway state becomes unreliable when Desktop is also connected"; MCP status "failed" examples.
- **[14] Kudryk video.** The transcript could not be re-read this run — the transcript tool returned "YouTube is blocking requests from your IP". The video's existence, title and author were verified through YouTube oEmbed (HTTP 200). The post therefore **paraphrases** the point (SSH backend = the terminal is outsourced; model requests still leave the laptop) and prints no quotation; the research pack's 2026-09-06 transcript read (community-19) is the basis of the paraphrase, and the technical claim itself is anchored to [8]. If a quotation is ever wanted, re-read the transcript first.
- **Issue #73082** (renderer/GPU 100 % CPU at idle, Apple Silicon, closed completed 2026-08-21) was fetched via `gh api` but is **not cited**: the table row is about the open Intel case #88275, and adding a fifteenth reference for a closed twin was not worth the length. The brief mentioned "#88275 / #73082"; the ledger records the second for completeness.
- Local cross-checks: `blog/hermes-security.html` (#4) states that under YOLO "Exactly two things survive it: your own `approvals.deny` rules, and the hardline blocklist" — consistent with [1] and with this post. `blog/hermes-desktop-fleet.html` (#10) carries the four-type Connections table, the 4401/4403 line, the #40656 caution, the `--setup-tcc-identity` line and the update order — this post links to it and repeats none of the table.

---

## Claims and numbers

| Value | Source | Where it lands |
|---|---|---|
| `approvals.mode` values `smart | manual | off`; default `smart` | [1] | §1, §2 step 2, `<pre>` 1, takeaways |
| `timeout: 300` seconds; unanswered prompt is denied (fail-closed) | [1] | §1, §2 step 5, takeaways |
| `cron_mode` / `single_query_mode` / `unattended_mode` default `deny` | [1] | §1, `<pre>` 1 |
| `mcp_reload_confirm: true`, `destructive_slash_confirm: true` | [1] | `<pre>` 1 (verbatim block) |
| YOLO via `hermes --yolo`, `/yolo`, `HERMES_YOLO_MODE=1`; banner `⚡ YOLO mode ON — all commands auto-approved`; `⚠ YOLO` status fragment | [1] | §2 step 6, takeaways |
| "flip YOLO on or off for just this session (matching the TUI)" | [2] | §2 step 6 |
| Blocklist rows: `rm -rf /`, fork bomb, `mkfs` on mounted root, `dd` to `/dev/sd*`, pipe untrusted URL to `sh`; survives `--yolo`, `mode: off`, cron `approve`, "allow always"; no override flag | [1] | §1 |
| `approvals.deny` examples `"git push --force*"`, `"*curl*|*sh*"`, `"dd if=* of=/dev/*"`; fnmatch, case-insensitive, host-reaching backends, consulted before YOLO | [1] | §2 step 4, `<pre>` 2, alert |
| CLI prompt choices once / session / always / deny; `rm -rf /tmp/…` = "recursive delete" | [1] | §2 step 7 |
| `hermes approvals suggest`; `--apply`; "`rm -rf build/` approved 100 times still never yields an `rm` entry" | [1] | §2 step 8 |
| Protected write paths (`~/.ssh/`, `~/.aws/`, `~/.kube/`, `/etc/sudoers`, `~/.netrc`, `auth.json`, `.env`, `mcp-tokens/`, `pairing/`, project `.env*`); no prompt, no chat override; `HERMES_WRITE_SAFE_ROOT` | [1] | §1 |
| Backend table: local, ssh keep the check; docker, singularity, modal, daytona, vercel_sandbox skip it | [1] | §1, §2 `<pre>` 3 |
| Execution-boundary sentence | [9] | §1 (quoted), §4 step 5, takeaways |
| "The only security boundary against an adversarial LLM is the operating system."; what terminal-backend isolation confines / does not confine | [8] | §1 blockquote, §2 alert |
| Safety listed among per-profile pages; contents undocumented; Applies to chip row behaviour | [2] | §2 intro, step 1 |
| Config changes take effect immediately (mtime-keyed cache) | [1] | §2 step 3 |
| v0.21.0 dry-run approval checker exists in release notes, absent from CLI reference | [10], [4] | §2 step 7 (hedged, no command printed) |
| Checkpoints opt-in, default off; `checkpoints.enabled: true`; `hermes chat --checkpoints`; defaults 20 / 500 MB / 10 MB; store `~/.hermes/checkpoints/store/`; one per directory per turn; triggers; 50,000-file skip; root/home skipped; git required; `/rollback`, `/rollback diff N`, `/rollback N`, `/rollback N --all`, `/rollback N <file>`; pre-rollback snapshot; agent-write ledger; `hermes checkpoints` / `prune` | [6] | §3 |
| `.env` block (`HERMES_DASHBOARD_BASIC_AUTH_USERNAME=admin`, `…PASSWORD=choose-a-strong-password`, `…SECRET=$(openssl rand -base64 32)`), `chmod 600`, `hermes serve --host 0.0.0.0 --port 9119`, `curl -s http://<host>:9119/api/status | jq '.auth_required, .auth_providers'` → `true` + `"basic"` | [2] | §4 `<pre>`, steps 2–3, table row 7 |
| OAuth preferred beyond own machine; `hermes dashboard register`; "Sign in with Nous Research"; username/password trusted-network only; Tailscale | [2] | §4 step 1 |
| Add connection: Name ≤ 64 chars, case-insensitive uniqueness; Gateway URL `http://homelab.lan:9119`; Session token / OAuth; SSH `user@host:22` + adopted token; Save connection; Test → "Reachable" probes HTTP + WebSocket; Primary semantics; gateway selector | [3] | §4 Desktop steps 1–5 |
| `HERMES_DESKTOP_REMOTE_URL` overrides the in-app URL; Connection mode → Remote gateway (Remote URL, Sign in, Save and reconnect); stay signed in only with `HERMES_DASHBOARD_BASIC_AUTH_SECRET` | [2] | §4 shortcuts |
| #40656 open since 2026-06-06, P3, 1 comment | [13] | §4 alert |
| `desktop.log` contents; `hermes logs desktop`, `hermes logs gui -f` | [2], [4] | table row 1, §6 |
| Reset commands `rm "$HOME/.hermes/hermes-agent/.hermes-bootstrap-complete"`, `rm -rf "$HOME/.hermes/hermes-agent/venv"`; `hermes doctor --fix` | [2], [4] | table row 1 |
| Electron ~114 MB from github.com; `retrying attempt=…`; self-heal via npmmirror.com; `ELECTRON_MIRROR` never overridden; `rm -f "$HOME/Library/Caches/electron"/electron-*.zip` / `"$HOME/.cache/electron"` | [2] | table row 2 |
| #89675: P1, v0.20.4 regression, spawned without `--profile`, closed completed 2026-08-23, web-dashboard workaround | [11] | table row 3 |
| TCC: grants keyed to signing identity; local builds carry a stable identifier-pinned signature; grants made before PR #73681 are cdhash-pinned and re-prompt while the toggle still reads ON; `tccutil reset All com.nousresearch.hermes`; `hermes desktop --setup-tcc-identity`; "Hermes Local Signing"; #95091 in v0.21.0 | [2], [10] | table row 4, takeaways |
| Windows: `hermes update` refuses while another `hermes.exe` holds the venv executable; v0.21.0 detached hand-off, "Windows desktop updates no longer park on 'Updating Hermes'" | [5], [10] | table row 5, §7 step 4 |
| #88275: 2019 MacBook Pro, v0.20.2, 40–73 % idle CPU, P2 open, `desktop.disable_gpu=true` roughly halves it (undocumented key) | [12] | table row 6 |
| Close codes 4401 (ticket did not authenticate) / 4403 (request guard, Host/peer mismatch); Settings → Gateways → Open logs | [7], [2] | table row 7 |
| SSH host key changed → `ssh-keygen -R <host>` → Retry | [2] | table row 8 |
| "Connection test failed" checks; WebSocket leg vs HTTP | [3] | table row 9 |
| Five log names and descriptions; `~/.hermes/logs/` or `<profile>/logs/`; `list`; `-n 200`; `--since 1h` | [4] | §6 `<pre>` |
| Error card layers and five actions; Open Desktop logs on remote/Cloud; Send diagnostics: explicit consent, redacted, Nous staff only, 14-day auto-delete, private link, backend + Desktop log bundled on remote | [2] | §6 |
| `hermes debug share --local`, `--nous`, `--no-redact`; uploads redacted by default | [4] | §6 |
| Private reporting; no public issues; no bug bounty; 90-day window | [8] | §6 blockquote, takeaways |
| Update affordances (Update now / ⌘K Update Hermes / toast); order backend → gateways → app; "Update desktop app" re-check | [2] | §7 |
| `hermes update --check`, `--plan`, `--backup`, `updates.pre_update_backup: full`; quick snapshot contents; receipts `~/.hermes/logs/update_receipts/latest.json`; `--keep-stash` and `git stash apply`; `⚠ Update partially complete`; rollback commands; `hermes config check`; Docker exit code 2 and `docker pull nousresearch/hermes-agent:latest` | [5] | §7 steps and `<pre>` |
| Update all instances behaviour per kind; "Managed by Hermes Cloud"; Docker/Nix refuse per row | [3] | §7 alert |
| Uninstall labels and CLI equivalents; app closes to finish; options hidden on GUI-only client; source-checkout removes `node_modules` and `apps/desktop/{dist,release}`; `--dry-run` | [2], [4] | §7 |
| `hermes backup` includes credentials; `hermes profile export` strips keys | [5] | §7 |

---

## Second verification pass — 2026-09-07, after the first run was interrupted

The first run was cut off by a rate limit before it could verify its own output, so every source was
re-fetched from scratch and every fact in the finished post was re-read against it. Fetch log for this
pass (`curl` to `raw.githubusercontent.com/NousResearch/hermes-agent/main/…`, all HTTP 200; GitHub REST
through `gh api` because anonymous `curl` to `api.github.com` returned 403 rate-limit again):

| Re-fetched | Bytes | Result |
|---|---|---|
| `website/docs/user-guide/security.md` | 44,275 | `approvals:` block, modes table, YOLO, blocklist, deny rules, write guards, seven-row backend table — all match the post |
| `website/docs/user-guide/desktop.md` | 54,936 | Safety listed-not-described, Applies-to, `.env` recipe, `curl /api/status`, Connection mode, error card, Send diagnostics, resets, Electron ~114 MB, TCC — all match |
| `website/docs/user-guide/multi-connection-desktop.md` | 18,027 | Add connection fields, Name max 64 + case-insensitive, `http://homelab.lan:9119`, `user@host:22` + adopted token, Test/"Reachable", Primary, Update all instances — all match |
| `website/docs/user-guide/checkpoints-and-rollback.md` | 11,340 | opt-in default off, `--checkpoints`, store path, 20/500/10, triggers, one-per-directory-per-turn, all five `/rollback` forms, pre-rollback snapshot, agent-write ledger, guards — all match |
| `website/docs/user-guide/features/web-dashboard.md` | 75,449 | line 202 carries the 4403 / 4401 close codes and "Settings → Gateways → Open logs" verbatim |
| `website/docs/reference/cli-commands.md` | 105,184 | five `hermes logs` names + files, `-n`/`-f`/`--since`/`list`, `hermes doctor [--fix]`, `debug share` flags, `uninstall [--full] [--gui] [--dry-run] [--yes]`, `hermes checkpoints prune` — all match; `approval-check` still absent, so the post's hedge stands |
| `website/docs/getting-started/updating.md` | 21,405 | `--check`/`--plan`/`--backup`, `pre_update_backup`, receipts, `--keep-stash`, both Windows guards, rollback commands, Docker exit code 2 — all match |
| `SECURITY.md` | 15,752 | §2.2 boundary sentence, §2.4 heuristics, §1 private reporting + no bug bounty, §5 90-day window, "What this confines / does **not** confine" — all match |
| `apps/desktop/README.md` | 10,404 | lines 146–148 are the execution-boundary sentence, quoted in the post word for word |
| `gh api …/issues/89675` | — | closed / completed, 2026-08-19 → 2026-08-23, `type/bug, P1, …, comp/desktop, area/profiles`, 21 comments, "v0.20.4 (2026.8.18)", "spawned **without the `--profile` flag**" — all match |
| `gh api …/issues/88275` | — | open, `type/perf, P2, comp/desktop`, 12 comments, 2026-08-17, MacBook Pro 2019 Intel, v0.20.2, "40-73% CPU continuously while idle", `desktop.disable_gpu=true` — all match |
| `gh api …/issues/40656` | — | open, `type/bug, comp/gateway, tool/mcp, P3`, 1 comment, 2026-06-06 — all match |
| `gh api …/pulls/73681` | — | NEW this pass: "fix(desktop): stable macOS signing identity so TCC grants survive local rebuilds", merged 2026-07-28T22:41:57Z — supports the corrected TCC row; the claim itself is carried by [2], which describes the fix and its stale-grant consequence |

### Corrections applied in this pass

| # | Was | Now | Source for the correction |
|---|---|---|---|
| 1 | §1 blockquote: "the repository's security policy **opens with** the sentence" | "names exactly one load-bearing boundary, and **§2.2** states it" | [8] — the sentence is in §2.2 "The Boundary: OS-Level Isolation"; §1 is "Reporting a Vulnerability" |
| 2 | §2 step 6 printed the YOLO banner as `⚡ YOLO mode ON — all commands auto-approved` | full string `⚡ YOLO mode ON — all commands auto-approved. Use with caution.`, plus the session-start red banner `⚠ YOLO mode — all approval prompts bypassed` alongside the `⚠ YOLO` status fragment | [1] — the toggle output and the two persistent reminders are three distinct strings; the post was quoting a truncation of one and omitting another |
| 3 | §4 `<pre>` was headed "verbatim from the desktop guide", but the block splices the docs' `.env` heredoc together with a `curl` taken from the Troubleshooting section, and condensed the docs' three-line comment into one | header is now plain `# on the remote host`, and the signing-secret comment is restored to the docs' own three lines | [2] — the heredoc and the `curl` line come from different sections of the page |
| 4 | §5 TCC row: symptom "after a rebuild"; cause "a locally rebuilt app can change identity" | symptom "macOS re-prompts … though System Settings still shows the toggle ON"; cause names the stable identifier-pinned signature as the normal case and the pre-#73681 cdhash-pinned grant as the failure; fix leads with `tccutil reset`, then `--setup-tcc-identity` for a certificate-anchored identity | [2] — "Locally built and self-updated apps are signed with a stable identifier-pinned ad-hoc signature, so grants persist across updates"; the old row implied the documented default is broken |
| 5 | §6 `<pre>`: `hermes debug share --local  # print the redacted support bundle instead of uploading it` | `# print the report to the terminal instead of uploading it` | [4] — redaction is an **upload-time** behaviour (`--no-redact` "Disable upload-time secret redaction"; "By default, **uploads** are redacted"); `--local` "Print the report locally instead of uploading", which is not the redacted bundle |

Nothing else in either sheet was changed. Element parity (h2 9 · h3 9 · pre 9 · table 1 · blockquote 2 ·
ul 1 · ol 5 · li 45 · key-takeaways 1 · table-wrapper 1 · FIGURE 1 · references__list 1) held across all
five edits, the EN sheet still contains zero Thai characters, and
`python3 scripts/build_series.py --series hermes-desktop --check` reports `hermes-desktop OK`.

### Re-checked and left alone (deliberate hedges the first run got right)

- **`hermes approval-check` / `hermes approvals test`.** Still contradictory between [10] and PR #81137, still absent from [4]. The post names no command; that is correct.
- **Settings → Advanced → Execution backend.** The research pack attributes this control, and the list `local, docker, singularity, modal, daytona, ssh`, to community walkthroughs (Kudryk, Wanderloots) whose transcripts could not be re-read this pass; [2] documents no such control. The post keeps "I could find no Desktop page that documents a control for it" and prints the seven values from [1]'s comparison table instead.
- **The Kudryk paraphrase in §2.** Still a paraphrase, still no quotation; the technical claim remains anchored to [8].
- **"any `.env` file anywhere on disk".** [1]'s row is `.env`, `.env.local`, `.env.production`, `.envrc` anywhere on disk — the post's phrasing is a fair reading and was left.

---

## Dated statuses (true as of 2026-09-07)

- Current release: Hermes Agent v0.21.0, tag `v2026.8.31`, published 2026-08-31T19:29:49Z. No separately versioned Desktop product; the post never quotes a Desktop version.
- #40656 open (P3, 1 comment, opened 2026-06-06). #88275 open (P2, opened 2026-08-17). #89675 closed as completed 2026-08-23. #73082 closed as completed 2026-08-21 (not cited).
- The docs Security page, desktop page, multi-connection page, CLI reference, updating page, checkpoints page, web-dashboard page and repo `SECURITY.md` / `apps/desktop/README.md` are all `main` as fetched on 2026-09-07.
- The `hermes approval-check` (release notes) / `hermes approvals test` (PR #81137) naming conflict is unresolved in the CLI reference on this date.
- PR #73681 (stable macOS signing identity) merged 2026-07-28; PR #95091 (TCC identity command) shipped in v0.21.0.

---

## Do not assert (looked for, could not verify)

- **What the Desktop's Settings → Safety page actually shows.** [2] lists the page among the per-profile pages and never describes it. The community tour in the research pack (Alex Hitt) lists "Approval mode, approval timeout, command allow-list, redact secrets, confirm MCP reloads", but that transcript was not re-read this run. The post says the page mirrors the config keys and names the keys.
- **A Desktop control for `terminal.backend` ("Settings → Advanced → Execution backend").** Not in [2]; the community angle attributes it to two videos not re-read this run. The post sets the key in `config.yaml` and says it found no Desktop page documenting a control.
- **Whether `/rollback` is documented for the Desktop chat.** [6] documents it as an in-session slash command and shows CLI output; the slash-commands page says it works in both CLI and gateway. The post says the Desktop chat is a session on the same backend and that no page read excludes it — it does not claim Desktop-specific behaviour.
- **`hermes approval-check` syntax.** Release notes name it `hermes approval-check`; the merged PR #81137 body names it `hermes approvals test` with `--json` / `--env-type` flags and exit codes 0/2/3; the CLI reference lists neither. No command is printed; the post says the checker is listed in the release notes but not in the CLI reference.
- **`desktop.disable_gpu`** as a documented key — not in configuration.md, desktop.md or environment-variables.md; the post calls it undocumented and attributes it to the reporter of #88275.
- **The web dashboard port in #89675's workaround (8787)** — conflicts with the documented default 9119; omitted.
- **A verbatim quotation from the Kudryk video** — transcript blocked this run; paraphrased only.
- **Any Nous statement on the Thai Ministry of Finance case, any CVE touching Desktop, any Desktop version number, any Hermes Cloud price, the "public preview" label** — none asserted; the Ministry case is linked to #4 and not retold.
- **That #89675's fix shipped in a specific tagged release** — the post says "closed as completed on 23 August 2026, before v0.21.0 — update"; it does not name the fixing commit or tag.
- **Intel Macs unsupported** — stated in the platform-support page, which was not re-fetched this run; the post points at #10's platform table instead of asserting it directly.
